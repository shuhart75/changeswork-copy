# Требования по feature — Формирование БТ из симуляции через AI-агента RAIN

Статус: **draft**  
Feature: `features/simulation-bt-agent/feature.md`  
Квартал: `2026-Q2`  
Дата обновления: `2026-04-28`  
Шаблон: `.workflow/templates/requirements/feature-requirements.template.md`

## Оглавление

Используются заголовки до уровня `####`.

## Общий контур feature

- Назначение feature: встроить в интерфейс симуляций окно агента для диалога и формирования БТ по данным симуляции.
- Что уже есть в baseline/current: список симуляций, деталка симуляции, вкладки деталки, форма редактирования, данные симуляции и существующая ролевая модель.
- Какая дельта добавляется этой feature: глобальная точка входа в окно агента, правая неблокирующая панель диалога, восстановление сессии по `session_id`, действие `Сформировать БТ`, публикация БТ, возврат `btUrl` для ручного копирования, аудит сценария и явное переиспользование уже реализованного simulation detail API как источника данных для BT-сценария.
- Какие slice входят в контрольный документ: `agent-entrypoint`, `dialog-session`, `bt-publication`.

## Порядок slice для контроля

1. `01 agent-entrypoint — Точка входа и контекст окна агента`
2. `02 dialog-session — Сессия диалога и работа окна агента`
3. `03 bt-publication — Действия по БТ, черновик запроса и публикация`

---

## STORY-SIMULATION-BT-AGENT-001 — Точка входа и контекст окна агента

Slice card: `slices/agent-entrypoint/slice.md`  
Детализация FE: `slices/agent-entrypoint/requirements/frontend.md`  
Детализация BE: `slices/agent-entrypoint/requirements/backend.md`  
Прототип: `slices/agent-entrypoint/delivery-prototype/prototype.html`  
Planning story: `planning/stories/STORY-SIMULATION-BT-AGENT-001.md`

**Бизнес-требования**

- Цель: дать пользователю единую точку входа в окно агента на страницах симуляций.
- Бизнес-правила: окно агента доступно со списка, деталки, вкладок деталки и формы редактирования; вызов открытия окна использует единый REST только с `session_id`; действие `Сформировать БТ` доступно только для конкретной успешно завершённой симуляции с опцией вывода в ПРОМ.
- Ограничения: открытие окна не должно блокировать основной интерфейс; при первом открытии фронт локально генерирует `session_id` в формате UUID и сохраняет его в рамках пользовательской сессии АС КОДА; при открытии окна клиент не передаёт историю, `contextPrompt`, ФИО пользователя и дату-время старта диалога.

**Пользовательские требования к АС КОДА**

- Пользователь может открыть окно агента с любой страницы раздела симуляций.
- Пользователь остаётся на текущей странице, а окно открывается без смены маршрута.
- На странице подходящей симуляции пользователь видит в окне агента доступное действие `Сформировать БТ`.

**Критерии приемки**

1. Окно агента открывается со списка симуляций, с деталки, с вкладок деталки и с формы редактирования.
2. При открытии или восстановлении окна в серверный вызов передаётся только `session_id`.
3. Для страниц без подходящей симуляции окно агента остаётся доступным, но действие `Сформировать БТ` недоступно.

**USE CASES**

- **осн. сценарий 1** пользователь открывает окно агента со списка симуляций.
- **альт. сценарий 1.1** пользователь открывает окно агента с деталки симуляции или с её вкладки.
- **альт. сценарий 1.2** пользователь открывает окно агента из формы редактирования симуляции.
- **осн. сценарий 2** фронт использует активный `session_id` и восстанавливает состояние окна.

### Функциональные требования

#### Реализация для FRONTEND

**Описание UI**

| Экран                                                    | Результат                                                                          |
| -------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| Список симуляций                                         | Доступна точка входа в окно агента                                                 |
| Деталка симуляции, вкладки деталки, форма редактирования | Доступна точка входа в окно агента без смены маршрута                              |
| Окно агента                                              | Отображается состояние сессии и действия по БТ в зависимости от контекста страницы |

Требования на фронт:

- добавить единую точку входа в окно агента на ключевых страницах раздела симуляций;
- открыть окно как неблокирующую панель без сброса состояния страницы;
- при первом открытии локально генерировать новый UUID `session_id`, а при повторном открытии переиспользовать текущий активный идентификатор;
- при открытии и восстановлении отправлять только `session_id`;
- сохранять `session_id` и дату-время старта диалога в рамках пользовательской сессии АС КОДА.

Связанный детальный FE pack: `slices/agent-entrypoint/requirements/frontend.md`

#### Реализация BACKEND

Требования на бэк:

- принять вызов открытия или восстановления окна только с `session_id`;
- при первом unseen значении `session_id` создавать новую серверную сессию, а при известном значении восстанавливать существующую;
- вернуть состояние окна и историю по `session_id`;
- обеспечить выдачу признаков доступности и данных для черновика запроса через существующие страницы симуляций;
- не принимать дополнительные клиентские данные в вызове открытия окна.

Связанный детальный BE pack: `slices/agent-entrypoint/requirements/backend.md`

|             |                                                            |
| ----------- | ---------------------------------------------------------- |
| **Сервис**  | `АС КОДА simulation host + agent integration boundary`     |
| ИФТ URL     | `POST /dialog/session`, существующие API страниц симуляций |
| База данных | `baseline/current/domain/aggregates/simulation.md`         |
| Методы      | `slices/agent-entrypoint/requirements/backend.md`          |

**Изменения в ролевой модели**

- Используется существующая ролевая модель доступа к страницам симуляций.
- Новая отдельная роль в рамках MVP не вводится.

**Отправка в Аудит**

- Должен фиксироваться факт открытия или восстановления окна агента.
- Должны фиксироваться ошибки открытия или восстановления сессии.

**BUGS (Если есть)**

- Пока не зафиксированы.

### Доп. пояснения

- Этот раздел задаёт вход в feature и должен оставаться первым в контрольной последовательности.
- Детальные правила по отображению и API вынесены в slice-паки и не должны расходиться с этим документом.

---

## STORY-SIMULATION-BT-AGENT-002 — Сессия диалога и работа окна агента

Slice card: `slices/dialog-session/slice.md`  
Детализация FE: `slices/dialog-session/requirements/frontend.md`  
Детализация BE: `slices/dialog-session/requirements/backend.md`  
Прототип: `slices/dialog-session/delivery-prototype/prototype.html`  
Planning story: `planning/stories/STORY-SIMULATION-BT-AGENT-002.md`

**Бизнес-требования**

- Цель: обеспечить устойчивый диалог с агентом внутри АС КОДА без блокировки остального интерфейса.
- Бизнес-правила: история и контекст диалога принадлежат агенту; открытие окна и отправка сообщения разделены; фронт локально генерирует UUID `session_id` при первом открытии окна, хранит его вместе с датой-временем старта диалога в пользовательской сессии АС КОДА и сбрасывает оба значения вместе с ней; backend создаёт серверную сессию при первом unseen `session_id`; при отправке сообщения передаются `user_prompt`, ФИО пользователя из СУДИР и дата-время старта диалога; статус агента показывается отдельным chip в шапке окна, а при ошибке пользователь видит причину и рекомендацию перезапустить сессию.
- Ограничения: клиентская история не является источником правды; Markdown должен корректно отображаться в сообщениях; текст пользовательского сообщения не валидируется и не интерпретируется со стороны АС КОДА.

**Пользовательские требования к АС КОДА**

- Диалог открывается в неблокирующей правой панели.
- Пользователь может продолжать работу со страницей симуляции вне панели агента.
- Пользователь может копировать текст из интерфейса АС КОДА в окно агента и из окна агента обратно в интерфейс АС КОДА.
- После ошибки пользователь видит понятное сообщение о проблеме агента и рекомендацию перезапустить сессию.

**Критерии приемки**

1. Окно агента работает как правая неблокирующая панель и не мешает навигации по интерфейсу.
2. История и состояние диалога восстанавливаются по `session_id`, который живёт в рамках пользовательской сессии АС КОДА.
3. При отправке текста в агент передаются ФИО пользователя из СУДИР и дата-время старта диалога.
4. Сообщения агента отображаются с поддержкой Markdown, а ошибка позволяет перезапустить сессию.

**USE CASES**

- **осн. сценарий 1** пользователь открывает окно, получает историю и продолжает переписку.
- **альт. сценарий 1.1** пользователь переходит между страницами симуляций, не закрывая окно.
- **альт. сценарий 1.2** во время ошибки пользователь перезапускает сессию и начинает новый диалог.
- **осн. сценарий 2** пользователь отправляет сообщение и получает обновлённую историю от агента.

### Функциональные требования

#### Реализация для FRONTEND

**Описание UI**

| Экран                    | Результат                                                                                                                  |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------------- |
| Правая панель агента     | В шапке отображается status chip агента; сразу под шапкой идёт область чата; ниже находится поле ввода и основные действия |
| Основной экран симуляций | Остаётся доступным вне панели агента                                                                                       |

Требования на фронт:

- реализовать правую боковую панель с историей сообщений и состояниями запроса;
- показывать в шапке окна chip статуса агента со значением `Онлайн` или `Ошибка`, а по клику раскрывать детали статуса и рекомендации;
- размещать область чата сразу под шапкой без дополнительных информационных карточек между шапкой и перепиской;
- поддержать локальную блокировку только элементов управления боковой панели на время отправки;
- отображать Markdown в сообщениях агента;
- обеспечить копирование текста в окно агента и из окна агента;
- использовать поле ввода высотой в 5 строк с вертикальной прокруткой при переполнении;
- при отправке сообщения передавать ФИО пользователя из СУДИР и дату-время старта диалога.

Связанный детальный FE pack: `slices/dialog-session/requirements/frontend.md`

#### Реализация BACKEND

Требования на бэк:

- хранить историю и контекст диалога на стороне агента;
- восстанавливать окно по `session_id`;
- принимать пользовательское сообщение отдельным вызовом после открытия окна;
- поддержать понятные ошибки и сценарий перезапуска сессии.

Связанный детальный BE pack: `slices/dialog-session/requirements/backend.md`

|             |                                                             |
| ----------- | ----------------------------------------------------------- |
| **Сервис**  | `RAIN dialog integration`                                   |
| ИФТ URL     | `POST /dialog/session`, `POST /dialog/{session_id}/message` |
| База данных | `по хранилищу сессий агента уточняется в detail pack`       |
| Методы      | `slices/dialog-session/requirements/backend.md`             |

**Изменения в ролевой модели**

- Ролевая модель не расширяется.
- Доступ к диалогу наследуется от точки входа feature.

**Отправка в Аудит**

- Должны аудироваться открытие или восстановление сессии, отправка сообщения, перезапуск сессии и ошибки агента.
- При необходимости сохраняются `session_id` и технический результат вызова.

**BUGS (Если есть)**

- Пока не зафиксированы.

### Доп. пояснения

- Этот раздел должен оставаться вторым в порядке контроля, потому что зависит от согласованной точки входа.
- Детальные требования по отображению, ошибкам и полям вызова описываются в slice-паках.

---

## STORY-SIMULATION-BT-AGENT-003 — Действия по БТ, черновик запроса и публикация

Slice card: `slices/bt-publication/slice.md`  
Детализация FE: `slices/bt-publication/requirements/frontend.md`  
Детализация BE: `slices/bt-publication/requirements/backend.md`  
Прототип: `slices/bt-publication/delivery-prototype/prototype.html`  
Planning story: `planning/stories/STORY-SIMULATION-BT-AGENT-003.md`

**Бизнес-требования**

- Цель: дать пользователю управляемый сценарий формирования БТ через окно агента и показать итоговую ссылку на созданный БТ для ручного копирования.
- Бизнес-правила: кнопка `Сформировать БТ` доступна только для конкретной успешно завершённой симуляции с опцией вывода в ПРОМ; клик по кнопке только вставляет черновик запроса; пользователь может произвольно редактировать текст или не отправлять его; под полем ввода расположены только основные действия `Сформировать БТ` и `Отправить`; после успешной публикации агент возвращает `btUrl` в окно агента; автоматическое сохранение ссылки на БТ в данных симуляции в MVP не выполняется.
- Ограничения: черновик запроса формируется на фронте по данным уже реализованного `GET /api/v1/simulation/{number}`; отдельный backend-метод или BT-specific подготовительный API для формирования черновика не вводится; автоматическая отправка текста не допускается; агент получает только тот текст, который пользователь отправил вручную.

**Пользовательские требования к АС КОДА**

- В подходящем контексте пользователь видит в окне агента кнопку `Сформировать БТ`.
- По клику пользователь получает черновик запроса в поле ввода и может его свободно менять.
- При наведении на кнопку пользователь видит подсказку с условиями доступности или с пояснением следующего шага.
- После успешной публикации пользователь видит `btUrl` в окне агента и может вручную скопировать ссылку.
- При желании пользователь сам сохраняет скопированную ссылку в соответствующем разделе симуляции вне автоматического сценария MVP.

**Критерии приемки**

1. Кнопка `Сформировать БТ` доступна только в подходящем контексте симуляции.
2. По клику на кнопку в поле ввода вставляется черновик запроса, который не отправляется автоматически.
3. После успешной публикации `btUrl` отображается в окне агента и доступен для ручного копирования.
4. В рамках MVP данные симуляции не обновляются автоматически ссылкой на БТ после публикации.

**USE CASES**

- **осн. сценарий 1** пользователь открывает окно из подходящей симуляции и вставляет черновик запроса.
- **альт. сценарий 1.1** окно открыто из неподходящей страницы, и действие `Сформировать БТ` недоступно.
- **альт. сценарий 1.2** пользователь редактирует черновик запроса или отказывается от отправки.
- **осн. сценарий 2** после успешной публикации пользователь получает ссылку на созданный БТ.

### Функциональные требования

#### Реализация для FRONTEND

**Описание UI**

| Экран              | Результат                                                                                                                                                                   |
| ------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Окно агента        | Под полем ввода отображаются кнопки `Сформировать БТ` и `Отправить`; для `Сформировать БТ` доступна контекстная tooltip-подсказка; после успеха в окне отображается `btUrl` |
| Страница симуляции | Автоматическое обновление ссылки на БТ после публикации в MVP не выполняется                                                                                                |

Требования на фронт:

- показать кнопку `Сформировать БТ` только в подходящем контексте;
- по клику вставлять в поле ввода черновик запроса по шаблону, используя данные уже реализованного `GET /api/v1/simulation/{number}`;
- выводить у `Сформировать БТ` tooltip с пояснением о доступности или о следующем шаге после вставки черновика;
- не отправлять черновик автоматически;
- после успеха отобразить `btUrl` в окне агента и дать пользователю вручную скопировать ссылку без автоматического обновления данных симуляции.

Связанный детальный FE pack: `slices/bt-publication/requirements/frontend.md`

#### Реализация BACKEND

Требования на бэк:

- переиспользовать уже реализованный `GET /api/v1/simulation/{number}` как источник признаков доступности действия по БТ и данных для черновика запроса;
- не вводить отдельный BT-specific backend-метод для подготовки черновика;
- принять ручную отправку текста пользователя и вернуть результат публикации;
- вернуть `btUrl` без автоматического сохранения в данных симуляции;
- аудировать успешную и неуспешную публикацию.

Связанный детальный BE pack: `slices/bt-publication/requirements/backend.md`

|             |                                                                        |
| ----------- | ---------------------------------------------------------------------- |
| **Сервис**  | `RAIN publication`                                                     |
| ИФТ URL     | `GET /api/v1/simulation/{number}`, `POST /dialog/{session_id}/message` |
| База данных | `новое автоматическое хранение btUrl в рамках MVP не требуется`        |
| Методы      | `slices/bt-publication/requirements/backend.md`                        |

**Изменения в ролевой модели**

- Дополнительные роли не вводятся.
- Проверка прав наследуется от точки входа и страниц симуляций.

**Отправка в Аудит**

- Обязателен аудит успешной и неуспешной публикации БТ.
- Отдельное событие автоматического обновления ссылки у симуляции в MVP не формируется, потому что такого обновления нет.

**BUGS (Если есть)**

- Пока не зафиксированы.

### Доп. пояснения

- Этот раздел завершает контрольную последовательность feature и должен идти после диалогового slice.
- Ссылка на БТ и аудит публикации считаются обязательной частью результата, но автосохранение ссылки в симуляцию не входит в MVP.
