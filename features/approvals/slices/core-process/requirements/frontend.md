# Интеграция согласования со SberDocs (Frontend)

Статус: **в работе**
Feature: `approvals`
Slice: `core-process`
Область: `MVP`
Дата обновления: `2026-05-27`
Шаблон: `.workflow/templates/requirements/frontend.template.md`
Decision ID: `DEC-2026-05-25-APPROVALS-SBERDOCS-001`

## Связь с feature-level документом

- Главный контрольный документ: `../../../requirements.md`
- Этот файл детализирует frontend-часть подготовки брифа, подписанта/получателей, отправки в SberDocs и read-only мониторинга.

## Назначение пакета

- Зафиксировать минимальный UI вместо собственной страницы согласований.
- Оставить в АС КОДА только подготовку данных и мониторинг статуса SberDocs.
- Исключить кнопки принятия решений и package UI из MVP.

## Источники и трассировка

### Основные источники

- `../slice.md`
- `../../../requirements.md`
- `../../../references.md`
- `requirements/backend.md`
- `context/source-materials/change-requests/sberdocs-approvals/Бриф для утверждения.md`
- `context/source-materials/change-requests/sberdocs-approvals/StateMachine_внутреннего_документа.md`
- `context/source-materials/change-requests/sberdocs-approvals/Цепочка_согласования_Сравнение_подходов.md`

### Связанные planning stories

- `STORY-APPROVALS-001` — требует отдельной синхронизации.

### Связанные артефакты

- Feature requirements: `../../../requirements.md`
- Domain impact: `../../../domain-impact.md`

## Контекст и бизнес-смысл

### Цель

Пользователь должен в АС КОДА подготовить бриф, подписанта и получателей, отправить документ в SberDocs и затем видеть текущий статус/историю без реализации собственного рабочего места согласующего.

### Пользователи и роли

- методолог, который отправляет документ в SberDocs и становится `author`;
- ПРМ, который передаётся в SberDocs как соавтор `additionalAuthorList[]`;
- согласующие не выбираются и не хранятся в АС КОДА; пользователь настраивает согласующих в SberDocs; подписант передаётся backend в SberDocs.

### Бизнес-контекст

Frontend больше не владеет approval actions. Он формирует данные для отправки и отображает read-only состояние, пришедшее из backend status snapshot SberDocs.

## Границы MVP

### Входит в MVP

- блок/вкладка `Согласование через SberDocs` на host screen;
- локальное редактирование подписанта и получателей в `new`-версии;
- локальное создание и редактирование JSON документа в `new`-версии;
- read-only блок скоркарты во всех состояниях;
- поле `Доп. эффекты` под блоком скоркарты до отправки;
- предпросмотр HTML-письма;
- создание новой версии при изменении брифа, подписанта или получателей до отправки либо после ошибки создания SberDocs-документа без `documentId`;
- подтверждение отправки в SberDocs;
- отображение `systemNumber`, ссылки на документ, mapped status, `stateName` и даты последней синхронизации;
- read-only статус документа из SberDocs и on-demand загрузка поимённой истории из approval sheet;
- блокировку действий доменного элемента, пока с ним связан `ApprovalChain`; вместо действий UI предлагает открыть документ в SberDocs;
- скачивание актуального DOCX из SberDocs после согласования;
- отображение ошибок создания/синхронизации.

### Не входит в MVP

- отдельная страница `Согласования`;
- вкладки `На согласовании` / `На утверждении`;
- UI-кнопки `approve`, `reject`, `ratify`, `sign` в АС КОДА;
- массовые действия;
- карточки пакетов и страница `Пакеты`;
- локальная frontend-агрегация статусов участников сверх данных SberDocs.

## Пользовательские сценарии

### Сценарий FE-1. Подготовка `new`-версии
1. Пользователь открывает host screen доменной сущности.
2. Если утверждающий не назначен, UI скрывает кнопку `Создать документ` и показывает скоркарту/`Доп. эффекты` read-only.
3. Если утверждающий назначен и документ ещё не создан, UI показывает кнопку `Создать документ`.
4. Пользователь заполняет поля документа и `Доп. эффекты`, затем сохраняет `new`-версию без запуска согласования.

### Сценарий FE-2. Предпросмотр письма и отправка
1. Пользователь открывает `Предпросмотр письма`.
2. UI отправляет текущий JSON документа и `Доп. эффекты` на backend.
3. UI показывает HTML письма, сформированный backend из JSON, скоркарты и `Доп. эффекты`.
4. Пользователь подтверждает отправку.
5. UI вызывает backend submit endpoint и блокирует повторную отправку до ответа.

### Сценарий FE-3. Мониторинг после отправки
1. После успешной отправки UI скрывает редактирование брифа, подписанта и получателей.
2. UI показывает `systemNumber`, ссылку на SberDocs, mapped status, оригинальный `documentState/stateName` и `lastSyncedAt`.
3. Методолог видит кнопку `Отправить уведомление`, пока backend не сообщил, что AV-уведомление уже отправлено вручную или автоматически.
4. UI показывает document status, `systemNumber` и ссылку; лист согласования загружает отдельным вызовом, когда пользователь открывает раздел истории.

### Сценарий FE-3a. Ручная отправка AV-уведомления
1. Методолог после submit нажимает `Отправить уведомление`.
2. UI вызывает backend endpoint ручной отправки и блокирует кнопку на время запроса.
3. После успеха UI показывает, что уведомление отправлено на `av@av.ru`, и больше не предлагает ручную отправку.
4. UI не выбирает получателей и не формирует перечень значимых участников: backend сам читает SberDocs approval sheet и справочник.

### Сценарий FE-4. Ошибка SberDocs
1. Backend возвращает `sberdocs_error` или validation error.
2. UI показывает понятное сообщение и технический код/описание из SberDocs; если health-check не `LIVING`, показывает, что SberDocs временно недоступен и отправка не начата.
3. Если документ не создан, `new`-версия остаётся редактируемой или пользователь создаёт новую версию и отправляет повторно; при health-check failure backend дополнительно уведомляет поддержку АС[СТ, РСП, КОДА, СРО].

### Сценарий FE-5. Отклонение в SberDocs
1. SberDocs вернул raw `REJECTED`.
2. UI отображает mapped status `on_approval`, raw `REJECTED`, ссылку `Открыть в SberDocs` и загружает комментарии из approval sheet отдельным методом.
3. UI не предлагает создать новую `new`-версию: правки и повторная отправка выполняются в SberDocs.

### Сценарий FE-6. Отзыв или редактирование в SberDocs
1. В интерфейсе АС КОДА кнопка `Отозвать` в MVP не показывается.
2. Если нужно отозвать или отредактировать уже созданный документ, пользователь открывает документ по ссылке `Открыть в SberDocs`.
3. В SberDocs действие выполняет методолог-автор, ПРМ-соавтор или заместитель автора.
4. После изменения состояния UI АС КОДА показывает результат только после синхронизации SberDocs status.
5. Если SberDocs возвращает `ON_DELETING` или `DELETED`, UI показывает local status `cancelled`, а не возвращает форму в `new`.

### Сценарий FE-7. Просмотр истории
1. Пользователь открывает раздел истории/листа согласования.
2. UI вызывает отдельный backend метод `approval-sheet`.
3. UI отображает поимённые решения и комментарии из SberDocs без ожидания, что они хранятся в основном snapshot.

### Сценарий FE-8. Скачать актуальный документ
1. После отправки/согласования пользователь нажимает `Скачать документ`.
2. UI вызывает backend метод получения актуального файла из SberDocs.
3. UI скачивает DOCX, который вернул SberDocs, а не локально сгенерированный preview.

## UI-состав и навигация

### Экран/состояние 1. New document and participants

- Назначение: подготовить всё, что уйдёт в SberDocs.
- Откуда открывается: host screen `Pilot` / `Deployment` / релевантная сущность.
- Что видно пользователю: поля документа, поле `Краткое содержание`, read-only подписант, получатели, read-only scorecard block, поле `Доп. эффекты`, preview email button, save new version, send to SberDocs.
- Какие действия доступны: `Создать документ`, `Сохранить`, `Сохранить и отправить на утверждение`, `Предпросмотр письма`, `Отмена`.

### Экран/состояние 2. SberDocs status snapshot

- Назначение: показать состояние после отправки.
- Что видно пользователю: локальный статус, `systemNumber`, URL, исходный `documentState`, `stateName`, `lastSyncedAt`, `approvalSheetId`, статус AV-уведомления и кнопка/раздел загрузки поимённой истории.
- Какие действия доступны: `Открыть в SberDocs`, `Обновить статус`, `Отправить уведомление`, `Скачать документ`. Кнопки `Отозвать` в MVP нет.

## Функциональные требования

### FE-FR-1. New-версия брифа и участников отправки

**Описание:**
Frontend должен редактировать бриф, подписанта и получателей в статусе `new` только до успешной отправки документа в SberDocs.

**Детали:**
- UI не показывает редактор этапов согласования и не сохраняет согласующих в локальной версии;
- UI должен явно донести, что согласующие и маршрут согласования настраиваются в SberDocs;
- поле `Краткое содержание` уходит в SberDocs `summary`;
- поля `Адресат`, `Наименование документа`, `Цель` обязательны при сохранении/отправке;
- в `Периметр применения` должно быть заполнено хотя бы одно поле;
- поле `Доп. эффекты` опционально, сохраняется в JSON, включается в письмо и не включается в DOCX;
- скоркарта отображается read-only во всех состояниях, включается в письмо и не включается в DOCX;
- preview письма доступен до отправки, не запускает процесс и не блокируется валидацией обязательных полей;
- после успешной отправки поля становятся read-only.

### FE-FR-2. Отправка в SberDocs

**Описание:**
UI не отправляет запрос в SberDocs напрямую, а вызывает backend АС КОДА.

**Детали:**
- submit button блокируется во время запроса;
- повторный submit запрещён backend, если версия уже отправлена;
- при `202 Accepted` UI закрывает диалог отправки и меняет статус `ApprovalChain` на значение из ответа;
- UI не получает и не отображает `jobId`, raw SberDocs response, DOCX metadata, brief/participants snapshot в ответ на submit; эти данные читаются только через обычный GET/status block при повторном открытии или обновлении статуса.

### FE-FR-3. Read-only status и on-demand history

**Описание:**
UI отображает status snapshot из backend, а историю участников получает отдельным методом при открытии раздела истории.

**Детали:**
- показывать mapped status и оригинальные поля SberDocs из status snapshot;
- показывать compact статус AV-уведомления, если backend возвращает marker `avNotification.sentAt/mode`; frontend не участвует в формировании письма и не выбирает получателей;
- автоматическое письмо backend отправляет на `av@av.ru` при `documentState = ON_APPROVAL` и активной задаче `APPROVAL/IN_WORK`, только если методолог ранее не отправил его вручную;
- не вычислять решения локально;
- не показывать action buttons согласующего;
- unmapped SberDocs status показывать как `Требует проверки` с оригинальным кодом;
- для unmapped статуса можно показывать пояснение, что backend уведомил поддержку, если такой признак пришёл в status snapshot;
- историю участников не ожидать в основном GET: для неё вызывать `GET /approval-sheet`.

### FE-FR-3a. Ручная отправка AV-уведомления

**Описание:**
Методолог может вручную отправить HTML-уведомление после submit, не дожидаясь автоматического триггера.

**Детали:**
- кнопка доступна после успешного submit в SberDocs, если backend не вернул признак уже отправленного AV-уведомления;
- UI вызывает backend `POST /send-notification`;
- UI не передаёт адресатов: письмо всегда отправляется на `av@av.ru`;
- UI не передаёт список значимых подписантов/согласовантов: backend сам читает `approval-sheet` и справочник;
- после успешной ручной отправки UI скрывает/блокирует кнопку, потому что автоматическая отправка больше не выполняется;
- если backend вернул `alreadySent`, UI показывает существующий статус отправки и не считает это ошибкой сценария.

### FE-FR-4. Отказ от отдельной страницы

**Описание:**
Навигация АС КОДА не должна вести пользователя на отдельную страницу `Согласования` для принятия решений.

**Детали:**
- ссылки из host screen ведут либо на SberDocs URL, либо остаются внутри host screen;
- вкладки `На согласовании`/`На утверждении` не создаются;
- package cards не отображаются.

### FE-FR-5. Отзыв через SberDocs

**Описание:**
UI АС КОДА не предоставляет действие `Отозвать` в MVP. Отзыв или редактирование уже созданного документа выполняется в интерфейсе SberDocs.

**Детали:**
- на host screen остаётся ссылка `Открыть в SberDocs`;
- UI может показывать пояснение, что автор документа в SberDocs — методолог-отправитель, а соавтор — ПРМ;
- после отзыва/редактирования в SberDocs UI получает новый статус только через sync/status refresh;
- повторное согласование из raw `REJECTED`, `ON_DELETING` или `DELETED` в АС КОДА не создаётся; `ON_DELETING`/`DELETED` отображаются как `cancelled`.

### FE-FR-5a. Блокировка действий доменного элемента

**Описание:**
Пока у доменного элемента есть связанный `ApprovalChain`, UI должен запретить действия с доменным элементом.

**Детали:**
- блокируются все action-кнопки host entity, кроме исключений, которые будут описаны отдельными требованиями;
- рядом со статусом согласования UI показывает пояснение: действие выполняется в SberDocs, если `documentId` уже получен;
- primary action для пользователя после создания SberDocs-документа — `Открыть в SberDocs`;
- действия локального approval block (`История`, `Скачать документ`, `Обновить статус`, подготовка `new` до отправки или после ошибки создания без `documentId`) не считаются действиями доменного элемента.

### FE-FR-6. Скачивание актуального документа

**Описание:**
UI должен скачивать документ из SberDocs через backend, потому что основной DOCX мог измениться в SberDocs после отправки.

**Детали:**
- действие доступно после появления `documentId`, а основной happy path — после `approved`;
- UI вызывает `GET /api/v1/approval-chains/{targetType}/{targetId}/document-file`;
- UI не пытается скачать локальный preview вместо документа SberDocs;
- при ошибке показывает, что актуальный документ временно недоступен, не меняя статус согласования.

## Интеграция с Backend API

| Метод и маршрут | Где используется | Что отправляем/читаем | Условия вызова | Примечание |
|---|---|---|---|---|
| `GET /api/v1/approval-chains/{targetType}/{targetId}` | host screen | active version + integration snapshot | при открытии вкладки/блока | внутренний API АС КОДА |
| `PUT /api/v1/approval-chains/{targetType}/{targetId}/new-version` | редактор `new`-версии | document JSON, `additionalEffects`, signer, recipients, change reason | до отправки в SberDocs | сохраняет `new`-версию или создаёт новую версию |
| `POST /api/v1/approval-chains/{targetType}/{targetId}/notification-preview` | предпросмотр письма | текущий document JSON + `additionalEffects` | до отправки, валидация не блокирует | backend добавляет скоркарту и возвращает HTML письма |
| `POST /api/v1/approval-chains/{targetType}/{targetId}/submit-to-sberdocs` | send action | подтверждённая версия; в ответе только `accepted`, `approvalChainId`, `versionId`, новый `status` | если версия ещё не отправлена и backend health-check SberDocs вернул `LIVING` | UI закрывает диалог и показывает новый статус; если health-check не `LIVING`, показывает ошибку без смены статуса |
| `POST /api/v1/approval-chains/{targetType}/{targetId}/send-notification` | ручное AV-уведомление | nothing | после submit, пока AV-уведомление ещё не отправлено | backend читает approval sheet/справочник, отправляет письмо на `av@av.ru`, сохраняет manual marker и отключает auto |
| `POST /api/v1/approval-chains/{targetType}/{targetId}/sync-sberdocs` | manual refresh | nothing или force flag | после отправки | backend обновляет status snapshot без загрузки истории |
| `GET /api/v1/approval-chains/{targetType}/{targetId}/approval-sheet` | раздел истории | nothing | когда пользователь открывает историю | backend читает approval sheet из SberDocs и не сохраняет локально |
| `GET /api/v1/approval-chains/{targetType}/{targetId}/document-file` | скачать документ | nothing | после появления `documentId`, особенно после согласования | backend получает актуальный основной DOCX из SberDocs |
| `POST /api/v1/approval-chains/{targetType}/{targetId}/resubmit` | repeat flow | reason | только после `sberdocs_create_error` без `documentId` | создаёт новую `new`-версию; из raw `REJECTED`, `ON_DELETING` и `DELETED` запрещён |

## Валидация на frontend

### Правила

- нельзя отправить документ без подписанта и получателя;
- нельзя отправить бриф без обязательных полей `Адресат`, `Наименование документа`, `Цель`;
- нельзя сохранить/отправить документ, если в `Периметр применения` не заполнено ни одно поле;
- нельзя повторно отправить уже созданный документ;
- нельзя редактировать `new`-версию после получения `documentId`.

### Сообщения об ошибках

| Ситуация | Сообщение | Где показываем |
|---|---|---|
| SberDocs validation error | `SberDocs не принял документ: <descriptionMessage>` | banner + field-level details when available |
| SberDocs health-check не `LIVING` | `SberDocs временно недоступен. Отправка не начата: <message>` | submit dialog + status block |
| sync unavailable | `Не удалось обновить статус SberDocs. Показаны последние сохранённые данные.` | status block |
| AV notification already sent | `Уведомление уже отправлено на av@av.ru.` | status block / button tooltip |
| AV notification failed | `Не удалось отправить уведомление на av@av.ru. Попробуйте позже.` | status block |
| unmapped status | `Получен неизвестный статус SberDocs: <code>. Поддержка уведомлена.` | status block |
| actual document unavailable | `Не удалось получить актуальный документ из SberDocs. Попробуйте позже или откройте документ в SberDocs.` | download action |

## Нефункциональные требования

- UI должен явно показывать источник статуса: `SberDocs`.
- UI должен сохранять введённую `new`-версию при ошибке создания документа.
- UI должен быть устойчив к временному отсутствию `approvalSheetId`; после его появления история загружается отдельным методом при открытии раздела истории.
- UI не должен создавать ощущение, что решение можно принять в АС КОДА.
- UI не должен обещать, что HTML-письмо совпадает с финальным DOCX: письмо и DOCX имеют разный состав, а финальный DOCX скачивается из SberDocs.
- UI не должен показывать выбор адресата AV-уведомления: адрес фиксирован на backend как `av@av.ru`.

## Чеклист тестирования

- [ ] `new`-версия сохраняется без отправки в SberDocs.
- [ ] Preview письма не меняет статус и открывается даже при незаполненных обязательных полях.
- [ ] Submit блокирует повторный submit.
- [ ] Submit требует подписанта, получателя, `Адресат`, `Наименование документа`, `Цель` и хотя бы одно поле `Периметр применения`.
- [ ] Если backend сообщает, что health-check SberDocs не `LIVING`, UI оставляет версию в `new` и показывает ошибку без создания job.
- [ ] После `COMPLETED` видны `documentId`, `systemNumber` и ссылка.
- [ ] `APPROVED` / `TRANSITION_TO_REGISTRATION` / `ON_REGISTRATION` / `REGISTRATION_REJECTED` / `REGISTERED` / `TRANSITION_TO_EXECUTION` / `ON_EXECUTION` / `EXECUTED` отображаются как согласовано.
- [ ] Raw `REJECTED` отображается как `on_approval` с предложением открыть SberDocs; UI не показывает создание новой `new`-версии.
- [ ] Raw `ON_DELETING` / `DELETED` отображает локальную цепочку как `cancelled`, а не возвращает в `new`.
- [ ] Поздний raw `CANCELLED` после `approved` не меняет отображение `approved`.
- [ ] Unknown/unmapped SberDocs status отображается как требующий проверки; если backend вернул признак support notification, UI сообщает, что поддержка уведомлена.
- [ ] Пока с доменным элементом связан `ApprovalChain`, action-кнопки доменного элемента заблокированы; UI предлагает перейти в SberDocs.
- [ ] Ошибка `FAILED` / `VALIDATION_ERROR` оставляет `new`-версию редактируемой или позволяет создать новую `new`-версию.
- [ ] Отдельная страница `Согласования` и package UI не доступны в MVP.
- [ ] Submit не передаёт внешние файлы/`attachmentList`, если пользователь явно не добавлял их в будущей версии.
- [ ] После отправки UI показывает напоминание установить признак `Коммерческая тайна (К2)` в SberDocs; submit не блокируется из-за К2.
- [ ] В блоке документа автором SberDocs отображается методолог-отправитель, а соавтором — ПРМ.
- [ ] После submit методолог видит кнопку ручной отправки AV-уведомления, если оно ещё не отправлено.
- [ ] Ручная отправка вызывает backend `send-notification`, после успеха показывает `av@av.ru` и скрывает/блокирует кнопку.
- [ ] UI не выбирает адресатов и не собирает список значимых участников для письма.
- [ ] UI скачивает актуальный DOCX через backend `document-file`, а не через локальный preview.
