# Внедрения (Frontend) — Форма создания и редактирования

Статус: **выделено из общего документа для отдельной задачи**  
Область: MVP  
Дата выделения: 2026-04-07  
Источник: [REQ_deployments_frontend.md](/home/reutov/Documents/AI/changesWork/final-spec/REQ_deployments_frontend.md)

## Бизнес-требования (контекст формы)

Реализовать Frontend для сущности `Внедрения`, чтобы пользователь мог:
- просматривать список внедрений;
- открывать детальную страницу внедрения;
- создавать и редактировать внедрение в статусе `draft`;
- управлять жизненным циклом внедрения;
- видеть связанные скоркарты, артефакты и блок `Связанные сущности`;
- видеть текущий статус согласования через связанный `approvalInstance`.

`Внедрение` для фронта рассматривается как доменная сущность с единственной рабочей карточкой и следующими состояниями:
- `draft`:
  - доступно редактирование;
  - доступно архивирование;
  - доступна отправка на согласование/утверждение;
- после отправки на утверждение:
  - UI не использует собственный промежуточные статусы ЖЦ;
  - UI отображает статус прикреплённого `approvalInstance`;
- `deployed`:
  - конечный статус;
- `archived`:
  - конечный статус.

Ключевое правило фронта:
- после отправки из `draft` состояние внедрения в списке, деталке и истории определяется по `approvalInstance.status`;
- фронт не дублирует и не переизобретает статусы процесса поверх `approvalInstance`.

### Определения

- `Внедрение`:
  доменная сущность, отображаемая пользователю как одна карточка со списком, детальной формой и формой редактирования.
- `Детальная форма внедрения`:
  экран просмотра текущего состояния внедрения и связанных данных без изменения полей.
- `Форма редактирования внедрения`:
  экран изменения полей внедрения и состава связанных скоркарт. Связанные с внедрением артефакты изменяются отдельно.
- `Блок "Связанные сущности"`:
  read-only блок, который показывает `Pilot` и `Simulation`, вычисленные backend через связанные скоркарты текущего внедрения.
- `Текущее состояние согласования`:
  состояние, определяемое по `approvalInstance.status` и отображаемое на русском языке в пользовательском интерфейсе.
- `Тип внедрения`:
  неизменяемая характеристика внедрения. В MVP доступны значения `Общий` и `Внедрение по результатам симуляции`.
- `Lineage симуляции`:
  отдельное поле в карточке внедрения, которое для типа `Внедрение по результатам симуляции` указывает на `Simulation`, результаты которой внедряются.

## Пользовательские требования

### ПТ-7. Форма создания

**Описание:** Пользователь должен иметь возможность создать внедрение.

**Детали:**
- в MVP форма создания и форма редактирования используют общий набор полей;
- форма создания должна включать:
  - название;
  - цель;
  - описание изменений;
  - периметр применения;
  - связанные скоркарты;
  - артефакты;
- критичность отображается как поле только для чтения и рассчитывается автоматически по связанным скоркартам;
- при создании фронт работает в два шага:
  - сначала вызывает `POST /api/v1/deployments` и получает `deployment.id`;
  - затем автоматически создаёт скоркарты и артефакты и завершает создание через `PUT /api/v1/deployments/{id}`;
- после успешного первого `POST` второй этап должен запускаться автоматически, без дополнительных действий пользователя;
- если второй этап не завершён, фронт должен показать сообщение об ошибке сохранения;
- если второй этап завершён успешно, фронт должен показать сообщение об успешном сохранении;
- после создания внедрение открывается в детальной форме;
- новое внедрение создаётся в статусе `draft`.
- тип внедрения выбирается пользователем внутри потока создания первой скоркарты из контекста внедрения;
- после первого успешного сохранения внедрения тип отображается только для чтения и больше не редактируется;
- если выбран тип `Внедрение по результатам симуляции`, пользователь обязан указать `Simulation` для поля `Lineage`.

### ПТ-8. Форма редактирования

**Описание:** Пользователь должен иметь отдельную, детально описанную форму редактирования внедрения.

**Детали:**
- форма редактирования доступна для статусов:
  - `draft`;
  - `approved`;
  - `ratified`;
  - `rejected`;
  - `cancelled`;
- при открытии формы поля должны быть предзаполнены текущими данными внедрения;
- форма должна содержать:
  - поле `Название`;
  - read-only поле `Тип внедрения`, если тип уже зафиксирован;
  - поле `Цель`;
  - поле `Описание изменений`;
  - поле `Периметр применения`;
  - поле `Lineage` до симуляции для типа `Внедрение по результатам симуляции`;
  - секцию `Критичность и целевые метрики`;
  - список связанных скоркарт;
  - управление артефактами;
  - кнопки `Сохранить` и `Отменить`;
- секция скоркарт должна поддерживать добавление и удаление элементов;
- для каждой редактируемой скоркарты должна поддерживаться списочная форма настроек:
  - номер элемента;
  - вычисленная критичность;
  - способ создания;
  - поля выбора источника;
  - название скоркарты;
  - действие раскрытия эффектов и метрик;
  - удаление элемента;
- форма должна поддерживать три способа создания скоркарты:
  - `Создать вручную`;
  - `На основе симуляции`;
  - `Использовать существующую`;
- при создании первой скоркарты из контекста внедрения интерфейс должен позволять выбрать тип внедрения:
  - `Общий`;
  - `Внедрение по результатам симуляции`;
- если выбран режим `На основе симуляции`, показывается поле выбора симуляции;
- если выбран режим `Использовать существующую`, показывается поле выбора существующей скоркарты;
- при выборе существующей скоркарты её название подставляется автоматически;
- если выбран тип `Внедрение по результатам симуляции`, поле выбора симуляции для `Lineage` обязательно и должно совпадать с симуляцией обязательной lineage-скоркарты;
- после первого успешного сохранения внедрения поле `Lineage` должно отображаться как read-only и не допускать смену симуляции;
- если внедрение уже имеет зафиксированный тип `Внедрение по результатам симуляции`, удаление обязательной lineage-скоркарты недоступно;
- для каждого элемента должен быть доступен разворот блока `Эффекты и метрики`;
- блок `Эффекты и метрики` должен содержать:
  - индикаторы изменений стратегии;
  - прогноз финансового эффекта;
  - дополнительные метрики;
- критичность должна пересчитываться автоматически на основе индикаторов риска;
- если в форме не выбрано ни одной скоркарты, должно отображаться пустое состояние с требованием добавить минимум одну скоркарту;
- если внедрение типа `Внедрение по результатам симуляции`, empty-state недопустим, пока не добавлена обязательная lineage-скоркарта;
- секция артефактов в режиме редактирования должна позволять:
  - видеть уже добавленные артефакты;
  - удалить артефакт;
  - добавить новый артефакт через отдельную строку ввода;
- кнопка `Отменить` должна сбрасывать несохранённые изменения и возвращать экран в режим просмотра.

### ПТ-9. Жизненный цикл внедрения

**Описание:** Интерфейс должен поддерживать жизненный цикл, заданный для MVP, без собственных промежуточных статусов.

**Детали:**
- жизненный цикл внедрения:
  - `draft`;
  - затем текущий статус прикреплённого `approvalInstance`;
  - `deployed`;
  - `archived`;
- из `draft` доступны действия:
  - `Редактировать`;
  - `Архивировать`;
  - `Отправить на согласование/утверждение`;
- после отправки внедрения UI отображает статус прикреплённого `approvalInstance`;
- для `approvalInstance.status = in_approval` доступно только действие `Отозвать с согласования`;
- для `approvalInstance.status = in_ratification` доступно только действие `Отозвать с согласования`;
- при отзыве из `in_approval` или `in_ratification` внедрение для MVP возвращается в `draft`;
- для `approvalInstance.status = approved` доступны:
  - `Отправить на утверждение`;
  - `Архивировать`;
  - `Редактировать`;
- если пользователь начинает редактирование из состояния `approved`, статус `approved` сбрасывается, и внедрение переходит в `draft`;
- для `approvalInstance.status = ratified` доступны:
  - `Внедрить`;
  - `Архивировать`;
  - `Редактировать`;
- если пользователь начинает редактирование из состояния `ratified`, статус `ratified` сбрасывается, и внедрение переходит в `draft`;
- для `approvalInstance.status` из группы `rejected` и `cancelled` доступны:
  - `Архивировать`;
  - `Редактировать`;
  - `Повторно отправить на согласование/утверждение`;
- при редактировании после `rejected` или `cancelled` внедрение возвращается в `draft`;
- действие `Внедрить` становится доступным только если backend вернул `deploy` в списке `available_actions`, что для MVP происходит после перехода `approvalInstance` в `ratified`;
- `deployed` является конечным статусом;
- `archived` является конечным статусом.

### ПТ-10. Роли и доступность действий

**Описание:** Интерфейс должен ограничивать действия по ролям.

**Детали:**
- просмотр списка и деталки доступен ролям, имеющим доступ к доменной модели;
- `prm` и `methodologist` видят внедрения всех продуктов;
- создание, редактирование, отправка на согласование, архивирование и внедрение доступны:
  - `prm` своего продукта;
  - `admin`;
- `methodologist` видит данные по всем продуктам, но не видит кнопок создания и изменения жизненного цикла;
- `approver` и `ratifier` не управляют внедрением с этой страницы.

## Критерии приемки

### КП-3. Форма редактирования

- [ ] Форма редактирования открывается для `draft`, `approved`, `ratified`, `rejected`, `cancelled`
- [ ] В режиме редактирования доступны поля названия, цели, описания изменений и периметра применения
- [ ] В режиме редактирования можно добавлять и удалять скоркарты
- [ ] Для скоркарты доступны режимы `manual`, `from_simulation`, `existing`
- [ ] Тип внедрения выбирается в потоке создания первой скоркарты
- [ ] После фиксации тип внедрения отображается как read-only и не меняется даже в `draft`
- [ ] После фиксации `Lineage` отображается как read-only и не меняется даже в `draft`
- [ ] Для типа `Внедрение по результатам симуляции` нельзя удалить обязательную lineage-скоркарту
- [ ] В режиме редактирования можно добавлять и удалять артефакты
- [ ] Кнопка `Отменить` откатывает локальные изменения

### КП-4. Жизненный цикл

- [ ] Из `draft` доступны редактирование, архивирование и отправка на согласование/утверждение
- [ ] После отправки статус внедрения определяется только по `approvalInstance`
- [ ] В `in_approval` доступен только отзыв с согласования
- [ ] В `in_ratification` доступен только отзыв с согласования
- [ ] Отзыв из `in_approval` и `in_ratification` возвращает внедрение в `draft`
- [ ] В `approved` доступны отправка на утверждение, архивирование и редактирование
- [ ] При редактировании из `approved` внедрение возвращается в `draft`
- [ ] В `ratified` доступны внедрение, архивирование и редактирование
- [ ] При редактировании из `ratified` внедрение возвращается в `draft`
- [ ] При `approvalInstance.status = ratified` backend возвращает действие `deploy` в `available_actions`
- [ ] Кнопка `Внедрить` отображается только если backend вернул `deploy` в `available_actions`
- [ ] В `rejected` и `cancelled` доступны архивирование, редактирование и повторная отправка
- [ ] `deployed` отображается как конечный статус
- [ ] `archived` отображается как конечный статус
- [ ] Интерфейс не использует собственный промежуточный статус поверх `approvalInstance`

## Функциональные требования

### ФТ-1. Адреса страниц

В MVP должны поддерживаться следующие адреса страниц:
- `/deployments`;
- `/deployments/:id`.

Правила:
- `/deployments` открывает списочную форму;
- `/deployments/:id` открывает детальную форму;
- форма редактирования открывается на адресе страницы `/deployments/:id` переключением страницы в режим редактирования;
- если внедрение находится в `in_approval`, `in_ratification`, `deployed` или `archived`, переход в режим редактирования должен быть запрещён.

### ФТ-4. Компонент формы редактирования

Компонент должен включать:
- редактирование `name`;
- отображение или выбор `deployment_type` по правилам формы;
- редактирование `goal`;
- редактирование `change_description`;
- редактирование `application_perimeter`;
- редактор связанных скоркарт;
- редактор артефактов;
- кнопки `Сохранить` и `Отменить`.

#### Требования к секции скоркарт

- форма внедрения должна позволять:
  - выбрать существующую скоркарту;
  - создать новую скоркарту вручную;
  - создать новую скоркарту на основе симуляции;
  - открыть редактирование уже выбранной скоркарты;
- при создании первой скоркарты из контекста внедрения форма должна зафиксировать `deployment_type`;
- для `deployment_type = simulation_based` форма должна:
  - выбрать `lineage`-симуляцию;
  - пометить соответствующую скоркарту как обязательную;
  - отключить удаление этой скоркарты;
- детальная структура формы скоркарты, её поля, режимы создания и правила расчёта критичности определяются требованиями [REQ_scorecards_frontend.md](/home/reutov/Documents/AI/changesWork/final-spec/REQ_scorecards_frontend.md);

### ФТ-5. Модель статусов

Frontend должен работать с такими отображаемыми состояниями:
Клиентская часть должна работать с такими отображаемыми состояниями:
- `draft`;
- любой допустимый `approvalInstance.status` после отправки;
- `deployed`;
- `archived`.

Правила:
- фронт не создаёт отдельный enum "статусов внедрения в процессе";
- для промежуточного состояния фронт просто отображает текущее состояние `approvalInstance`;
- во всех пользовательских местах интерфейса статус должен отображаться на русском языке;

### ФТ-6. Действия жизненного цикла

Поддерживаемые действия интерфейса:
- `submit_for_approval`;
- `start_ratification`;
- `recall`;
- `deploy`;
- `archive`.

Матрица доступности:

| Отображаемое состояние | Действия |
|---|---|
| `draft` | `edit`, `submit_for_approval`, `archive` |
| `in_approval` | `recall` |
| `in_ratification` | `recall` |
| `approved` | `edit`, `start_ratification`, `archive` |
| `ratified` | `edit`, `deploy`, `archive` |
| `rejected` | `edit`, `submit_for_approval`, `archive` |
| `cancelled` | `edit`, `submit_for_approval`, `archive` |
| `deployed` | нет |
| `archived` | нет |

Примечания:
- при `edit` из `approved`, `ratified`, `rejected` и `cancelled` внедрение должно быть возвращено в `draft`;
- для внедрения отзыв из `in_approval` и `in_ratification` возвращает состояние в `draft`;
- если `ApprovalInstance` различает `approval_rejected`/`ratification_rejected` и `approval_cancelled`/`ratification_cancelled`, фронт должен отображать их в группах `rejected` и `cancelled` по одинаковым правилам доступности.


## Описание модели данных интерфейса

```typescript
type DeploymentTerminalStatus = 'deployed' | 'archived';

type DeploymentUiStatus =
  | 'draft'
  | DeploymentTerminalStatus
  | 'in_approval'
  | 'in_ratification'
  | 'approved'
  | 'ratified'
  | 'rejected'
  | 'cancelled'
  | string; // любой approvalInstance.status

type DeploymentCriticality = 'high' | 'low' | null;

interface DeploymentArtifactVm {
  id?: string;
  title: string;
  type: 'business_case' | 'rov' | 'other';
  url: string;
}

interface DeploymentScorecardItemVm {
  id: string;
  name: string;
  criticality?: DeploymentCriticality;
  isRequired?: boolean;
  creationMode?: 'manual' | 'from_simulation' | 'existing';
  sourceSimulationId?: string | null;
  existingScorecardId?: string | null;
  showEffects?: boolean;
  effects?: {
    financial: Record<string, unknown>;
    additionalMetrics: Array<Record<string, unknown>>;
    strategyIndicators: Record<string, boolean>;
    criticality?: 'high' | 'low';
  };
}

interface DeploymentVm {
  id: string;
  displayId: string;
  name: string;
  deploymentType: 'general' | 'simulation_based' | null;
  goal: string | null;
  changeDescription: string | null;
  applicationPerimeter: string | null;
  productId: string;
  productName: string;
  status: DeploymentUiStatus;
  approvalInstanceStatus?: string | null;
  criticality: DeploymentCriticality;
  availableActions?: string[];
  createdBy: string;
  createdAt: string;
  deployedAt: string | null;
  lineageSimulation?: {
    id: string;
    displayId: string;
    name: string;
    status: string;
    url: string;
  } | null;
  scorecards: DeploymentScorecardItemVm[];
  relatedSimulations: Array<{
    entityType: 'simulation';
    id: string;
    displayId: string;
    name: string;
    status: string;
    url: string;
    isLineage?: boolean;
  }>;
  relatedPilots: Array<{
    entityType: 'pilot';
    id: string;
    displayId: string;
    name: string;
    status: string;
    url: string;
  }>;
  artifacts: DeploymentArtifactVm[];
}
```

## Интеграция с Backend API

### Используемые endpoint'ы

- `POST /api/v1/deployments`
- `PUT /api/v1/deployments/{id}`
- `POST /api/v1/deployments/{id}/action`

### Требования к контракту

- действие `submit_for_approval` должно создавать или привязывать `approvalInstance`;
- фронт ожидает, что после отправки backend вернёт актуальный `approvalInstance.status`;
- фронт ожидает, что при `approvalInstance.status = ratified` backend вернёт действие `deploy` в `available_actions`;
- действие `start_ratification` переводит `approved` в `in_ratification` или иной согласованный ratification-статус процесса;
- действие `recall` переводит внедрение из `in_approval` или `in_ratification` обратно в `draft`;
- действие `deploy` переводит внедрение в `deployed`;
- действие `archive` переводит внедрение в `archived`.

## Валидация

### Форма создания и редактирования

- `name` обязательно;
- `goal`, `changeDescription`, `applicationPerimeter` опциональны;
- `deploymentType` обязателен на момент финального сохранения;
- должна быть выбрана минимум одна скоркарта;
- для нового артефакта обязательны:
  - `title`;
  - `url`;
- если выбран режим `from_simulation`, `sourceSimulationId` обязателен;
- если выбран режим `existing`, `existingScorecardId` обязателен;
- если `deploymentType = simulation_based`, `lineageSimulation` обязателен;
- если `deploymentType = simulation_based`, обязательная lineage-скоркарта должна оставаться в составе формы;
- поле `criticality` вручную не редактируется.

## Обработка ошибок

- ошибки сохранения формы должны показываться без потери введённых данных;
- при успешном завершении создания или редактирования фронт должен показывать короткое уведомление `Внедрение сохранено`;
- если создание или редактирование не завершилось, фронт должен показывать короткое уведомление `Не удалось сохранить внедрение`;
- если backend запрещает редактирование не-`draft` внедрения, фронт должен показать понятное сообщение и вернуть пользователя в режим просмотра.
