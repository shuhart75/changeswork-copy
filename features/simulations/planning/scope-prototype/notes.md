# Заметки к scope prototype

Этот feature-контейнер создан для нормализации existing coverage, а не для новой planning-дельты.

## Цель
- дать видимый feature-level контейнер для simulation scope;
- не потерять traceability между baseline/current и legacy task docs.

## Какой идеальный сценарий показываем
- полный workspace симуляции: список, форма, деталка, lifecycle, артефакты и связанные сущности.

## Какой MVP cut показываем отдельно
- отдельный MVP cut сейчас не выделяется: фиксируем уже deployed baseline behavior.

## Какие planning stories обсуждаем через этот прототип
- planning stories для этой imported feature не поднимались.

## Mock data
- если понадобится reconstruction прототипа, исходники нужно брать из `context/source-materials/legacy/changeswork-full/`.

## Открытые вопросы
- нужен ли отдельный reconstruction prototype для simulation scope или baseline-level docs достаточно.
