# Внедрения — Списочная форма (Frontend)

Статус: **для передачи команде**
Область: MVP
Дата обновления: 2026-03-31

## Оглавление
1. [Бизнес-требования](#бизнес-требования)
2. [Пользовательские требования к АС КОДА](#пользовательские-требования-к-ас-кода)
3. [Критерии приемки](#критерии-приемки)
4. [Функциональные требования](#функциональные-требования)
5. [Описание UI-модели данных](#описание-ui-модели-данных)
6. [Интеграция с Backend API](#интеграция-с-backend-api)
7. [Обработка ошибок](#обработка-ошибок)

## Бизнес-требования

### Цель

РеализоватьFrontend для сущности `Внедрения` в части списковой формы, чтобы пользователь мог:
- просматривать список внедрений;
- переходить в детальную страницу по клику на строку.

### Контекст

Ключевое правило фронта:
- после отправки из `draft` состояние внедрения в списке определяется по `approvalInstance.status`;
- фронт не дублирует и не переизобретает статусы процесса поверх `approvalInstance`.

### Определения

- `Внедрение`:
  доменная сущность, отображаемая пользователю как одна карточка со списком, детальной формой и формой редактирования.
- `Тип внедрения`:
  неизменяемая характеристика внедрения. В MVP доступны значения `Общий` и `Внедрение по результатам симуляции`.

## Пользовательские требования к АС КОДА

### ПТ-1. Общая структура страницы (часть «Списочная форма»)

**Описание:** Раздел `Внедрения` должен поддерживать списочную форму.

**Детали:**
- `Списочная форма`:
  - таблица внедрений;
  - фильтр по продукту;
  - переход в деталку по клику на строку.

### ПТ-2. Списочная форма

**Описание:** Пользователь должен видеть табличный список внедрений в логике, показанной в прототипе.

**Детали:**
- список отображается в виде таблицы;
- в верхней части страницы есть фильтр продукта:
  - `Все продукты`;
  - отдельные вкладки по каждому продукту;
- каждая строка списка кликабельна и открывает деталку внедрения;
- в списке должны отображаться:
  - `display_id` внедрения;
  - название;
  - статус;
  - критичность;
  - автор создания;
  - дата создания;
  - дата внедрения;
- статус в списке:
  - для `draft` показывается `Черновик`;
  - после отправки показывается текущий статус `approvalInstance`, но в русскоязычном отображении;
  - для конечных состояний показывается `Внедрено` или `Архив`;
- критичность отображается цветным чипом;
- если дата внедрения отсутствует, отображается `—`.

## Критерии приемки

### КП-1. Список

- [ ] Страница списка показывает табличную форму внедрений
- [ ] Работает фильтр по продукту через верхний переключатель
- [ ] Каждая строка списка открывает детальную форму
- [ ] В списке отображаются ID, название, статус, критичность, автор, дата создания и дата внедрения
- [ ] После отправки из `draft` статус отображается по `approvalInstance.status`, но в русскоязычном пользовательском лейбле

## Функциональные требования

### ФТ-1. Адреса страниц (часть «Списочная форма»)

В MVP должны поддерживаться следующие адреса страниц:
- `/deployments`;

Правила:
- `/deployments` открывает списочную форму.

### ФТ-2. Компонент списочной формы

Компонент должен включать:
- заголовок страницы;
- переключатель продуктов;
- таблицу внедрений.

#### Колонки таблицы

- `ID`
- `Название`
- `Тип` (опционально, если помещается в MVP-таблицу без ухудшения читабельности)
- `Статус`
- `Критичность`
- `Создал`
- `Создано`
- `Развернуто`

### ФТ-5. Модель статусов (часть «Списочная форма»)

Frontend должен работать с такими отображаемыми состояниями в списке:
- `draft`;
- любой допустимый `approvalInstance.status` после отправки;
- `deployed`;
- `archived`.

Правила:
- фронт не создаёт отдельный enum "статусов внедрения в процессе";
- для промежуточного состояния фронт просто отображает текущее состояние `approvalInstance`;
- во всех пользовательских местах интерфейса статус должен отображаться на русском языке.

## Описание UI-модели данных (поля, используемые в списке)

Для табличной формы из полной модели `DeploymentVm` используются следующие поля:

```typescript
type DeploymentUiStatus =
  | 'draft'
  | 'deployed'
  | 'archived'
  | 'in_approval'
  | 'in_ratification'
  | 'approved'
  | 'ratified'
  | 'rejected'
  | 'cancelled'
  | string; // любой approvalInstance.status

type DeploymentCriticality = 'high' | 'low' | null;

interface DeploymentListItemVm {
  id: string;
  displayId: string;
  name: string;
  deploymentType: 'general' | 'simulation_based' | null;
  status: DeploymentUiStatus;
  approvalInstanceStatus?: string | null;
  criticality: DeploymentCriticality;
  createdBy: string;
  createdAt: string;
  deployedAt: string | null;
  productId: string;
  productName: string;
}
```

## Интеграция с Backend API

### Используемые endpoint'ы

- `GET /api/v1/deployments`

### Требования к контракту

- список внедрений должен возвращать данные, достаточные для табличной формы;
- для каждого элемента списка должны быть доступны поля: `id`, `display_id`, `name`, `deployment_type`, `status`, `criticality`, `created_by`, `created_at`, `deployed_at`, `product_id`, `product_name`, `approval_instance_status`.

## Обработка ошибок

- ошибки загрузки списка должны отображаться отдельным сообщением на странице списка.
