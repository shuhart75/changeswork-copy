# Задачи для страницы "Создание новой инициативы" (Create Initiative)

## Обзор страницы

**Страница:** Форма создания инициативы (Create Initiative Form)
**Путь в макете:** Кнопка "Создать инициативу" на странице списка инициатив
**Статус:** Новая страница, требует доработки БД и аналитики

### Что показывает страница (из макета):

**Форма создания со следующими полями:**
- Название инициативы (обязательное, текстовое поле)
- Продукт (обязательное, dropdown)
- Описание (опциональное, многострочное текстовое поле)
- Артефакты (опциональное, компонент ArtifactsManager)

**Функционал:**
- Валидация обязательных полей (название, продукт)
- Кнопка "Отмена" - возврат к списку инициатив
- Кнопка "Создать инициативу" - сохранение и переход к детальному просмотру
- Скрытие вкладок фильтров по продуктам при открытии формы

**Права доступа:**
- ПРМ может создавать инициативы только для своего продукта
- Методолог НЕ может создавать инициативы
- Админ может создавать инициативы для любого продукта

**Связь с другими сущностями:**
- Инициатива принадлежит одному Product (обязательно)
- Артефакты могут быть прикреплены сразу при создании

---

## AN-CD1: Аналитика страницы "Создание новой инициативы"

**Тип:** Story
**Приоритет:** Высокий
**Оценка:** 5 дней

### Summary
Детализация требований и проектирование UI/UX для формы создания инициативы с валидацией и правами доступа

### Description

Провести аналитику и детализацию требований для страницы создания инициативы:

1. **Детализация бизнес-требований:**
   - Уточнить правила доступа для разных ролей (ПРМ видит только свой продукт в dropdown)
   - Определить правила валидации полей
   - Определить правила работы с артефактами при создании
   - Уточнить поведение после успешного создания (переход к детальному просмотру)
   - Определить формат display_id (INIT-XXX) и правила генерации

2. **Спецификация UI/UX:**
   - Wireframe формы создания
   - Спецификация валидации полей (клиентская и серверная)
   - Дизайн сообщений об ошибках
   - Адаптивность (desktop/tablet/mobile)
   - Состояния кнопки "Создать" (disabled при незаполненных обязательных полях)

3. **Спецификация API контракта:**
   - POST /api/initiatives/ - создание новой инициативы
   - Параметры запроса (name, description, product_id, artifacts[])
   - Формат ответа (созданный объект с id, display_id, created_at, created_by)
   - Коды ошибок (400 - валидация, 403 - нет прав, 500 - серверная ошибка)
   - GET /api/products/ - список продуктов для dropdown (с учетом прав доступа)

4. **Определение полей формы:**
   - Название (обязательное, min 3 символа, max 255 символов)
   - Продукт (обязательное, dropdown с фильтрацией по правам доступа)
   - Описание (опциональное, max 2000 символов)
   - Артефакты (опциональное, файлы + ссылки)

5. **Правила валидации:**
   - Название: обязательное, min 3 символа, max 255 символов, уникальное в рамках продукта
   - Продукт: обязательное, должен быть в списке доступных для пользователя
   - Описание: опциональное, max 2000 символов
   - Артефакты: опциональное, валидация типов файлов и размера

6. **Права доступа:**
   - ПРМ видит в dropdown только свой продукт
   - Админ видит все продукты
   - Методолог НЕ имеет доступа к этой странице (HTTP 403)

7. **Поведение после создания:**
   - Успешное создание → переход к детальному просмотру созданного внедрения
   - Ошибка валидации → показать ошибки под соответствующими полями
   - Серверная ошибка → показать toast с сообщением об ошибке

### Acceptance Criteria

- [ ] Бизнес-требования задокументированы и согласованы
- [ ] Wireframe формы готов (Figma/Sketch или описание)
- [ ] API контракт задокументирован (OpenAPI спецификация или описание)
- [ ] Определены все поля формы с типами данных и ограничениями
- [ ] Правила валидации описаны (клиентская и серверная)
- [ ] Права доступа для ролей определены
- [ ] Правила генерации display_id определены
- [ ] Поведение после создания описано
- [ ] Адаптивность для разных разрешений учтена
- [ ] Документ согласован с заказчиком

### Dependencies
- AN-D1 (требуется понимание структуры Initiative)

### Deliverables
- Документ "Спецификация формы создания инициативы" (markdown/confluence)
- API контракт для POST /api/initiatives/
- Wireframe или описание UI

---

## BE-CD1: Backend для страницы "Создание новой инициативы"

**Тип:** Story
**Приоритет:** Высокий
**Оценка:** 2 дня

### Summary
Реализация API endpoint для создания инициативы с валидацией и правами доступа

### Description

Реализовать Backend API для создания инициативы:

1. **Миграция БД (если требуется):**
   - Добавить constraint на уникальность name в рамках product_id
   - Добавить индексы для оптимизации (product_id, created_at, created_by)
   - Проверить наличие поля display_id с автогенерацией (INIT-XXX)

2. **API Endpoint: POST /api/initiatives/**

   **Параметры запроса:**
   ```json
   {
     "name": "Внедрение новой скоркарты Premium Q1 2026",
     "description": "Краткое описание целей и задач внедрения",
     "product_id": "credit_cards",
     "artifacts": [
       {
         "type": "file",
         "name": "strategy_doc.pdf",
         "url": "https://storage/files/abc123"
       }
     ]
   }
   ```

   **Формат ответа (201 Created):**
   ```json
   {
     "id": "uuid",
     "display_id": "DEP-042",
     "name": "Внедрение новой скоркарты Premium Q1 2026",
     "description": "Краткое описание целей и задач внедрения",
     "product": {
       "id": "credit_cards",
       "label": "Кредитные карты"
     },
     "status": "draft",
     "created_at": "2026-03-06T10:00:00Z",
     "created_by": {
       "id": "user-uuid",
       "full_name": "Иван Иванов"
     },
     "artifacts": []
   }
   ```

3. **Бизнес-логика:**
   - Валидация: name обязательное, min 3 символа, max 255 символов
   - Валидация: product_id обязательное, должен существовать в БД
   - Валидация: name уникальное в рамках product_id
   - Автогенерация display_id (INIT-XXX, последовательная нумерация)
   - Автоматическая установка status = 'draft'
   - Автоматическая установка created_by = текущий пользователь
   - Создание связей с артефактами (если переданы)

4. **Права доступа:**
   - Middleware проверяет роль пользователя
   - ПРМ может создавать только для своего продукта (product_id = user.product_id)
   - Админ может создавать для любого продукта
   - Методолог НЕ может создавать инициативы (HTTP 403)
   - HTTP 403 если ПРМ пытается создать для чужого продукта

5. **API Endpoint: GET /api/products/**
   - Список продуктов для dropdown
   - Фильтрация по правам доступа:
     - ПРМ видит только свой продукт
     - Админ видит все продукты
   - Формат ответа:
   ```json
   [
     {
       "id": "credit_cards",
       "label": "Кредитные карты"
     }
   ]
   ```

6. **Обработка артефактов:**
   - Создание записей в таблице artifacts
   - Связь с initiative через initiative_id
   - Валидация типов файлов (pdf, doc, docx, xls, xlsx)
   - Валидация размера файлов (max 50MB)
   - Валидация URL для ссылок

### Acceptance Criteria

- [ ] Миграция БД применена (если требуется)
- [ ] Constraint на уникальность name в рамках product_id создан
- [ ] Индексы созданы для оптимизации
- [ ] POST /api/initiatives/ endpoint реализован
- [ ] Валидация name (обязательное, min 3, max 255, уникальное в рамках продукта)
- [ ] Валидация product_id (обязательное, существует в БД)
- [ ] Автогенерация display_id (INIT-XXX) работает
- [ ] Автоматическая установка status = 'draft'
- [ ] Автоматическая установка created_by = текущий пользователь
- [ ] Права доступа реализованы (ПРМ только свой продукт, методолог 403)
- [ ] HTTP 403 возвращается при попытке создать для чужого продукта
- [ ] GET /api/products/ endpoint реализован с фильтрацией по правам
- [ ] Обработка артефактов реализована (создание, валидация)
- [ ] HTTP 400 возвращается при ошибках валидации с детальными сообщениями
- [ ] HTTP 201 возвращается при успешном создании
- [ ] Unit тесты покрывают все сценарии (coverage > 80%)
- [ ] Integration тесты для прав доступа
- [ ] API документация обновлена (Swagger/OpenAPI)
- [ ] Postman коллекция с примерами запросов

### Dependencies
- AN-CD1 (требуется API контракт)
- BE-D1 (таблица initiative должна существовать)
- BE-1 (базовая структура БД)

### Technical Notes

**Пример валидации (Django):**
```python
from django.core.exceptions import ValidationError
from django.db import models

class Initiative(models.Model):
    name = models.CharField(max_length=255)
    product = models.ForeignKey('Product', on_delete=models.PROTECT)
    
    class Meta:
        constraints = [
            models.UniqueConstraint(
                fields=['product_id', 'name'],
                name='unique_initiative_name_per_product'
            )
        ]
    
    def clean(self):
        if len(self.name) < 3:
            raise ValidationError("Название должно содержать минимум 3 символа")
```

**Автогенерация display_id:**
```python
def generate_display_id(product_id):
    last_initiative = Initiative.objects.filter(
        product_id=product_id
    ).order_by('-display_id').first()
    
    if last_initiative and last_initiative.display_id:
        last_number = int(last_initiative.display_id.split('-')[1])
        new_number = last_number + 1
    else:
        new_number = 1
    
    return f"DEP-{new_number:03d}"
```

**Права доступа (Django REST Framework):**
```python
from rest_framework import permissions

class InitiativeCreatePermission(permissions.BasePermission):
    def has_permission(self, request, view):
        user = request.user
        
        if user.role == 'methodologist':
            return False
        
        if user.role == 'admin':
            return True
        
        if user.role == 'prm':
            product_id = request.data.get('product_id')
            return product_id == user.product_id
        
        return False
```

---

## FE-CD1: Frontend для страницы "Создание новой инициативы"

**Тип:** Story
**Приоритет:** Высокий
**Оценка:** 3 дня

### Summary
Реализация UI формы создания инициативы с валидацией и интеграцией с API

### Description

Реализовать Frontend для формы создания инициативы:

1. **Компонент CreateInitiativeForm:**
   - Форма с полями: название, продукт, описание, артефакты
   - Responsive дизайн (desktop/tablet/mobile)
   - Валидация на клиенте (обязательные поля, длина)
   - Disabled состояние кнопки "Создать" при незаполненных обязательных полях

2. **Поля формы:**
   - **Название инициативы:**
     - TextField, fullWidth, required
     - Placeholder: "Например: Внедрение новой скоркарты Premium Q1 2026"
     - Валидация: min 3 символа, max 255 символов
     - Error message под полем при ошибке валидации
   
   - **Продукт:**
     - Select dropdown, fullWidth, required
     - Загрузка списка продуктов из API (GET /api/products/)
     - Фильтрация по правам доступа (ПРМ видит только свой продукт)
   
   - **Описание:**
     - TextField, fullWidth, multiline, rows={4}
     - Placeholder: "Краткое описание целей и задач внедрения"
     - Max 2000 символов
   
   - **Артефакты:**
     - Компонент ArtifactsManager (уже существует в макете)
     - Поддержка файлов и ссылок

3. **Кнопки действий:**
   - **Отмена:**
     - Button, variant="outlined"
     - Возврат к списку инициатив (setShowCreateInitiative(false))
   
   - **Создать внедрение:**
     - Button, variant="contained"
     - Disabled если не заполнены обязательные поля (название, продукт)
     - При клике → POST /api/initiatives/
     - Loading состояние во время запроса

4. **API интеграция:**
   - InitiativeService.create(data)
   - ProductService.getList() - для загрузки продуктов
   - Обработка ошибок (400, 403, 500)
   - Toast уведомления при ошибках

5. **Валидация:**
   - Клиентская валидация перед отправкой:
     - Название: обязательное, min 3, max 255
     - Продукт: обязательное
     - Описание: max 2000 символов
   - Серверная валидация:
     - Обработка ошибок 400 с детальными сообщениями
     - Показ ошибок под соответствующими полями

6. **Поведение после создания:**
   - Успешное создание (201) → переход к детальному просмотру созданного внедрения
   - Показать toast "Внедрение успешно создано"
   - Обновить список внедрений (если пользователь вернется назад)

7. **Loading и Error состояния:**
   - Skeleton loader при загрузке продуктов
   - Loading spinner на кнопке "Создать" во время запроса
   - Error message при ошибке загрузки продуктов
   - Toast уведомления при ошибках API

8. **Скрытие вкладок фильтров:**
   - При открытии формы создания (showCreateInitiative = true) вкладки фильтров по продуктам должны скрываться
   - Уже реализовано в макете через условие в ProductFilterBar

### Acceptance Criteria

- [ ] CreateInitiativeForm компонент реализован
- [ ] Все поля формы отображаются корректно
- [ ] Валидация обязательных полей работает (название, продукт)
- [ ] Валидация длины названия работает (min 3, max 255)
- [ ] Dropdown продуктов загружается из API
- [ ] Фильтрация продуктов по правам доступа работает (ПРМ видит только свой)
- [ ] Поле описания работает (multiline, max 2000)
- [ ] ArtifactsManager интегрирован
- [ ] Кнопка "Отмена" возвращает к списку
- [ ] Кнопка "Создать" disabled при незаполненных обязательных полях
- [ ] POST запрос отправляется с правильными данными
- [ ] Loading состояние кнопки "Создать" работает
- [ ] Успешное создание → переход к детальному просмотру
- [ ] Toast "Внедрение успешно создано" показывается
- [ ] Ошибки валидации (400) отображаются под полями
- [ ] Ошибка 403 (нет прав) показывает toast
- [ ] Skeleton loader при загрузке продуктов
- [ ] Error state при ошибке загрузки продуктов
- [ ] Responsive дизайн работает (desktop/tablet/mobile)
- [ ] Вкладки фильтров скрываются при открытии формы
- [ ] Unit тесты для компонента (coverage > 70%)
- [ ] TypeScript типы определены

### Dependencies
- AN-CD1 (требуется UI спецификация)
- BE-CD1 (требуется готовый API endpoint)
- FE-D1 (требуется InitiativeService)
- FE-1 (базовая структура проекта)
- FE-2 (UI Kit компоненты: TextField, Select, Button)
- FE-4 (AuthContext для проверки прав)

### Technical Notes

**Пример структуры компонента:**
```typescript
interface CreateInitiativeFormProps {
  onCancel: () => void;
  onSave: (initiative: Initiative) => void;
}

function CreateInitiativeForm({ onCancel, onSave }: CreateInitiativeFormProps) {
  const [initiativeName, setInitiativeName] = useState('');
  const [initiativeDescription, setInitiativeDescription] = useState('');
  const [selectedProduct, setSelectedProduct] = useState('');
  const [artifacts, setArtifacts] = useState<Artifact[]>([]);
  const [products, setProducts] = useState<Product[]>([]);
  const [loading, setLoading] = useState(false);
  const [errors, setErrors] = useState<Record<string, string>>({});

  useEffect(() => {
    fetchProducts();
  }, []);

  const handleSave = async () => {
    const newErrors: Record<string, string> = {};
    if (!initiativeName || initiativeName.length < 3) {
      newErrors.name = 'Название должно содержать минимум 3 символа';
    }
    if (!selectedProduct) {
      newErrors.product = 'Выберите продукт';
    }
    
    if (Object.keys(newErrors).length > 0) {
      setErrors(newErrors);
      return;
    }

    setLoading(true);
    try {
      const initiative = await InitiativeService.create({
        name: initiativeName,
        description: initiativeDescription,
        product_id: selectedProduct,
        artifacts: artifacts
      });
      
      showToast('Инициатива успешно создана', 'success');
      onSave(initiative);
    } catch (error) {
      if (error.status === 400) {
        setErrors(error.data);
      } else if (error.status === 403) {
        showToast('У вас нет прав для создания инициативы для этого продукта', 'error');
      } else {
        showToast('Ошибка при создании инициативы', 'error');
      }
    } finally {
      setLoading(false);
    }
  };

  return (
    <Box sx={{ width: '100%' }}>
      <Paper sx={{ p: 3, mb: 3 }}>
        <Typography variant="h6" sx={{ mb: 3 }}>
          Создание новой инициативы
        </Typography>
        <Grid container spacing={3}>
          <Grid item xs={12}>
            <TextField
              fullWidth
              label="Название инициативы"
              value={initiativeName}
              onChange={(e) => setInitiativeName(e.target.value)}
              required
              error={!!errors.name}
              helperText={errors.name}
            />
          </Grid>
          <Grid item xs={12} md={6}>
            <FormControl fullWidth required error={!!errors.product}>
              <InputLabel>Продукт</InputLabel>
              <Select
                value={selectedProduct}
                onChange={(e) => setSelectedProduct(e.target.value)}
                label="Продукт"
              >
                {products.map(product => (
                  <MenuItem key={product.id} value={product.id}>
                    {product.label}
                  </MenuItem>
                ))}
              </Select>
            </FormControl>
          </Grid>
          <Grid item xs={12}>
            <TextField
              fullWidth
              label="Описание"
              value={initiativeDescription}
              onChange={(e) => setInitiativeDescription(e.target.value)}
              multiline
              rows={4}
            />
          </Grid>
          <Grid item xs={12}>
            <ArtifactsManager artifacts={artifacts} onChange={setArtifacts} />
          </Grid>
        </Grid>
      </Paper>
      <Paper sx={{ p: 2 }}>
        <Stack direction="row" spacing={2} justifyContent="flex-end">
          <Button variant="outlined" onClick={onCancel}>
            Отмена
          </Button>
          <Button
            variant="contained"
            onClick={handleSave}
            disabled={!initiativeName || !selectedProduct || loading}
          >
            {loading ? <CircularProgress size={24} /> : 'Создать внедрение'}
          </Button>
        </Stack>
      </Paper>
    </Box>
  );
}
```

---

## QA-CD1: Тестирование страницы "Создание новой инициативы"

**Тип:** Story
**Приоритет:** Высокий
**Оценка:** 2 дня

### Summary
Комплексное тестирование функционала создания инициативы: API endpoints, валидация, права доступа, UI/UX

### Description

Провести полное тестирование функционала создания инициативы:

1. **API тестирование:**
   - **POST /api/initiatives/**
     - Успешное создание с минимальными данными (name, product_id)
     - Успешное создание с полными данными (включая description, artifacts)
     - Валидация обязательных полей (name, product_id)
     - Валидация длины name (min 3, max 255)
     - Валидация уникальности name в рамках product_id
     - Автогенерация display_id (INIT-XXX)
     - Автоматическая установка status = 'draft'
     - Автоматическая установка created_by
     - HTTP 201 при успешном создании
     - HTTP 400 при ошибках валидации
     - HTTP 403 при отсутствии прав
   
   - **GET /api/products/**
     - Список продуктов для ПРМ (только свой продукт)
     - Список продуктов для админа (все продукты)
     - HTTP 403 для методолога

2. **Тестирование прав доступа:**
   - **ПРМ:**
     - Может создать инициативу для своего продукта
     - Не может создать инициативу для чужого продукта (HTTP 403)
     - Видит в dropdown только свой продукт
   
   - **Методолог:**
     - Не имеет доступа к странице создания (HTTP 403)
     - Не видит кнопку "Создать инициативу" в UI
   
   - **Админ:**
     - Может создать инициативу для любого продукта
     - Видит все продукты в dropdown

3. **UI тестирование:**
   - **Отображение формы:**
     - Все поля отображаются корректно
     - Placeholder текст присутствует
     - Required маркеры на обязательных полях
     - Вкладки фильтров по продуктам скрыты
   
   - **Валидация на клиенте:**
     - Кнопка "Создать" disabled при пустом названии
     - Кнопка "Создать" disabled при невыбранном продукте
     - Error message под полем при ошибке валидации
     - Валидация длины названия (min 3, max 255)
   
   - **Динамическая загрузка:**
     - Dropdown продуктов загружается при открытии формы
     - Skeleton loader показывается при загрузке продуктов
   
   - **Кнопки действий:**
     - Кнопка "Отмена" возвращает к списку инициатив
     - Кнопка "Создать" отправляет POST запрос
     - Loading spinner на кнопке во время запроса
     - Кнопка disabled во время запроса

4. **Интеграционное тестирование:**
   - **Успешный сценарий:**
     - Заполнить форму → нажать "Создать" → получить 201 → переход к детальному просмотру
     - Toast "Внедрение успешно создано" показывается
     - Созданное внедрение появляется в списке
   
   - **Сценарий с ошибкой валидации:**
     - Заполнить форму с невалидными данными → нажать "Создать" → получить 400 → ошибки под полями
     - Исправить ошибки → повторить отправку → успех
   
   - **Сценарий с ошибкой прав:**
     - ПРМ пытается создать для чужого продукта → получить 403 → toast с сообщением об ошибке

5. **Тестирование артефактов:**
   - Создание без артефактов
   - Создание с файлами
   - Создание со ссылками
   - Валидация типов файлов
   - Валидация размера файлов (max 50MB)

6. **Responsive тестирование:**
   - Desktop (1920x1080)
   - Tablet (768x1024)
   - Mobile (375x667)
   - Все элементы доступны и кликабельны
   - Текст читаем на всех разрешениях

7. **Тестирование граничных случаев:**
   - Название ровно 3 символа (минимум)
   - Название ровно 255 символов (максимум)
   - Описание ровно 2000 символов (максимум)
   - Дублирование названия в рамках одного продукта
   - Одинаковое название для разных продуктов (должно работать)

8. **Тестирование производительности:**
   - Время загрузки формы < 1 сек
   - Время загрузки продуктов < 500ms
   - Время отправки формы < 2 сек

9. **Регрессионное тестирование:**
   - Список инициатив работает после создания нового
   - Детальный просмотр работает для созданного внедрения
   - Фильтры по продуктам работают после создания
   - Поиск находит созданное внедрение

### Acceptance Criteria

- [ ] Все API endpoints протестированы (POST, GET products)
- [ ] Валидация полей работает корректно (клиентская и серверная)
- [ ] Права доступа работают для всех ролей (ПРМ, методолог, админ)
- [ ] UI отображается корректно на всех разрешениях
- [ ] Динамическая загрузка продуктов работает
- [ ] Кнопки действий работают корректно
- [ ] Успешный сценарий работает end-to-end
- [ ] Сценарии с ошибками обрабатываются корректно
- [ ] Артефакты создаются и валидируются
- [ ] Граничные случаи обработаны
- [ ] Производительность соответствует требованиям
- [ ] Регрессионное тестирование пройдено
- [ ] Все найденные баги задокументированы и исправлены
- [ ] Test report подготовлен с результатами тестирования

### Dependencies
- AN-CD1 (требуется спецификация для тест-кейсов)
- BE-CD1 (требуется готовый API для тестирования)
- FE-CD1 (требуется готовый UI для тестирования)


### Test Cases

**TC-CD-001: Успешное создание внедрения с минимальными данными**
- Preconditions: Пользователь с ролью ПРМ авторизован
- Steps:
  1. Открыть страницу списка инициатив
  2. Нажать "Создать инициативу"
  3. Ввести название "Тестовое внедрение"
  4. Выбрать продукт из dropdown
  5. Нажать "Создать инициативу"
- Expected: HTTP 201, переход к детальному просмотру, toast "Внедрение успешно создано"

**TC-CD-002: Валидация обязательных полей**
- Preconditions: Пользователь на странице создания инициативы
- Steps:
  1. Оставить поле "Название" пустым
  2. Попытаться нажать "Создать инициативу"
- Expected: Кнопка disabled, нельзя отправить форму

**TC-CD-003: ПРМ не может создать для чужого продукта**
- Preconditions: Пользователь с ролью ПРМ (product_id = "credit_cards")
- Steps:
  1. Открыть страницу создания инициативы
  2. Проверить dropdown продуктов
- Expected: В dropdown только "Кредитные карты", другие продукты недоступны

**TC-CD-004: Методолог не имеет доступа**
- Preconditions: Пользователь с ролью методолог
- Steps:
  1. Попытаться открыть страницу создания инициативы
- Expected: HTTP 403 или редирект, кнопка "Создать" не отображается

**TC-CD-005: Валидация уникальности названия**
- Preconditions: Существует внедрение "Тест" для продукта "Кредитные карты"
- Steps:
  1. Создать новое внедрение с названием "Тест" для того же продукта
- Expected: HTTP 400, ошибка "Внедрение с таким названием уже существует для этого продукта"

**TC-CD-006: Одинаковое название для разных продуктов**
- Preconditions: Существует внедрение "Тест" для продукта "Кредитные карты"
- Steps:
  1. Создать новое внедрение с названием "Тест" для продукта "Потребительские кредиты"
- Expected: HTTP 201, успешное создание (разные продукты)

---

## Итоговая зависимость задач

```
AN-CD1 (2 дня)
  ↓
BE-CD1 (3 дня)
  ↓
FE-CD1 (2 дня)
  ↓
QA-CD1 (2 дня)
```

**Общая длительность (критический путь):** 9 дней

**Параллельная работа:**
- BE-CD1 может начаться сразу после AN-CD1
- FE-CD1 может начаться только после готовности BE-CD1 (нужен работающий API)
- QA-CD1 начинается после завершения FE-CD1 (нужен готовый UI)

---

## ✅ Ключевые особенности страницы "Создание новой инициативы"

1. **Новая страница:** Требует полной реализации (аналитика, бэкенд, фронтенд, тестирование)
2. **Доработка БД:** Constraint на уникальность name в рамках product_id, автогенерация display_id
3. **Права доступа:** ПРМ только свой продукт, методолог не может создавать, админ все продукты
4. **Валидация:** Клиентская и серверная, уникальность названия в рамках продукта
5. **Автогенерация:** display_id (INIT-XXX), status = 'draft', created_by = текущий пользователь
6. **Артефакты:** Можно прикрепить сразу при создании
7. **Скрытие фильтров:** Вкладки фильтров по продуктам скрываются при открытии формы
8. **Переход после создания:** Автоматический переход к детальному просмотру созданного внедрения
9. **Упрощение для MVP:** Без подпродуктов, только продукты
